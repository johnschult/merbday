<products>
  <product id="<%= @product.id %>">
    <property name="name"><%= @product.name %></property>
    <property name="sku"><%= @product.sku %></property>
    <property name="aisle"><%= @product.aisle %></property>
    <property name="bin"><%= @product.bin %></property>
    <property name="inventory"><%= @product.inventory %></property>
    <property name="updated_at"><%= @product.updated_at %></property>
  </product>
</products>
